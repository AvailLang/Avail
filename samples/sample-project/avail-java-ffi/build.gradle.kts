plugins {
    java
}

group = "org.availlang.sample"
version = "1.0"
description = "Java foreign function interface (FFI) to be accessed using " +
    "Avails Pojos"

repositories {
    mavenCentral()
}

